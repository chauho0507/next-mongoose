export enum ETabs {
  Learning = 'learning',
  Wedding = 'wedding',
  Nails = 'nails',
}

export enum EStatuses {
  NotStart,
  Progressing,
  Pending,
  Done,
  Cancelled,
}

export enum EDataTypes {
  String = 'string',
  Number = 'int',
  Boolean = 'boolean',
  Date = 'date',
  Array = 'array',
  BigInt = 'bigint',
}

export enum EHeaderActions {
  Profile = 'profile',
  Logout = 'logout',
}
